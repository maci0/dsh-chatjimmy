# dsh-chatjimmy

# Local-dev overlay: absolute path, per the first-plugin tutorial. For the
# live-reload install, paste the package-name row from cordis.patch.yml into
# ~/.dsh/profiles/<profile>/cordis.patch.yml instead.
- insert:
    - id: chatjimmy
      name: '/home/maci/dsh-plugins/dsh-chatjimmy/src/index.ts'
      config:
        model: llama3.1-8B
        topK: 8
